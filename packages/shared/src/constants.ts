/** Стоимость публикации заказа в тиынах (50 ₸) */
export const ORDER_PUBLISH_COST_TIYN = 5_000;

/** Стоимость отклика поставщика в тиынах (50 ₸) */
export const OFFER_COST_TIYN = 5_000;

/** Стоимость ИИ-разбора в тиынах (1 000 ₸) */
export const AI_PARSE_COST_TIYN = 100_000;

/** Минимальная сумма пополнения в тиынах (1 000 ₸) */
export const MIN_TOPUP_TIYN = 100_000;

/** Максимальный размер файла в байтах (20 МБ) */
export const MAX_FILE_SIZE_BYTES = 20 * 1024 * 1024;

/** Валюта по умолчанию */
export const DEFAULT_CURRENCY = 'KZT';

/** Тиынов в тенге */
export const TIYN_PER_TENGE = 100;
